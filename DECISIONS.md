# DECISIONS — SHELF LIFE full build (t_e029b0c5)

- D1: Full build lives in-repo at the ROOT of sxaad69/shelf-life (main branch); W1 feeler preserved at `feelers/index.html` (moved, not deleted). Spec says "Godot 2D" build; repo law says every game is a repo — one repo, two eras.
- D2: Deploy topology = shadow-form-spike pattern: web build output goes to a `gh-pages` branch (build/ never committed on main, pinned rule 10); Pages source switched from `main /` to `gh-pages /` at deploy. Playable URL stays https://sxaad69.github.io/shelf-life/.
- D3: Engine = godot 4.3 stable headless CLI (installed, templates 4.3.stable present), renderer gl_compatibility (house standard from rulebreaker/set-piece-master presets).
- D4: Core sim is a single pure-logic script `scripts/shelf_state.gd` (no node deps) so the generator/solver/audit are headless-testable; UI scripts only render + feed it. Feeler's exact rule semantics carried over verbatim (scan order = per bay front→back, bays left→right; WEIGHT front≥back per bay; ADJACENCY category positions contiguous in scan; FIFO expiry non-decreasing along scan; FACING all faceOut).
- D5: Generator = answer key by construction (category pair per bay, expiry monotonic along scan, front≥back weight, all faceOut) then scramble; independent brute-force solver must find ≥1 solution per seed or seed is rejected/advanced — same contract as feeler.
- D6: Staged difficulty (pulse caveat): L1-2 = FACING+WEIGHT only (2 rules, 4 items/2 bays), L3-4 = +ADJACENCY (3 rules, 6 items/3 bays), L5+ = all 4 rules (8 items/4 bays). Rule badges shown = only the ACTIVE rules for the level; audit checks only active rules.
- D7: Flip affordance (pulse caveat): every unflipped item shows a visible "FLIP" chip; flipped items show a "flipped" face glyph; right-click AND a click-on-chip both flip. Footer hint retained but affordance is in-item, not footer-only.
- D8: Audio = jsfxr node tooling copied from set-piece-master/tools (jsfxr 1.4.1, 22050Hz WAV → ffmpeg OGG 22kHz mono), files land in assets/audio/ with provenance in assets/assets.json. Files: place_thock, badge_chime, shop_bell, shop_loop, spoil_buzz, hint_ping.
- D9: Rewarded placement hook = Hint button: highlights ONE violating item (pulse: highlight-one-violating-item) with 3 hints per level; Skip-delivery removes one tray item (re-solves: generator guarantees solvability with any subset — verified by solver re-run; if no solution, skip is refused). Ad-SDK call site stubbed `AdGateway.show_rewarded()` for later portal SDK wiring (no SDK in build).
- D10: Thumbnail = rule-badge-forward SVG->PNG (badges dominating frame, shelf mini-scene behind), generated deterministically via Python+Pillow; lands in assets/branding/ + cover/. Launch condition per spec.
- D11: Rule 8 monetization set: revenue model + genre benchmarks written to docs/monetization.md BEFORE release gate; build itself ships ad-hook stubs only.
- D12: Tests: `tests/` run headless via a `--script` harness (determinism across seeds, solver finds ≥1 solution for N seeds, audit agrees with solver verdict on solved/permuted states, difficulty stages activate correct rule subsets).
- D13: QA bridge (web-only): main.gd publishes full state + live control rects to `window.__SL_STATE` via JavaScriptBridge after layout (deferred 2 frames). Enabled rule-16 playthrough: real CDP/X11 clicks through actual UI, no state injection.
- D14: Playthrough found a real defect — item cards had MOUSE_FILTER_IGNORE so tray clicks passed through (game unplayable by mouse). Fixed to STOP; caught exactly as pinned rule 16 intends.
- D15: Click input for QA runs = CDP Input.dispatchMouseEvent at client coords derived from bridge rects (design→client via canvas getBoundingClientRect + Godot 'keep' letterbox math). xdotool also works but CDP is deterministic. Headless=new chromium does NOT boot Godot WebGL reliably; xvfb kiosk + swiftshader flags do.
- D16: Deploy verification chain: gh-pages tip == local build commit; Pages source branch switched main→gh-pages; curl 200 + title "Shelf Life". Note: GitHub auto-gzips wasm on-wire (35MB → 8MB), matching shadow-form-spike behavior.

